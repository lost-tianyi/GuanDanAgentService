## 1. 布局：对局页主画布满宽与纵向吃满

- [x] 1.1 审视 `GameView` / `GameBoard` / `game-area` 链路的 `max-width`、`margin`、`padding`、`height`/`min-height`，去掉或调整限制 **1200px** 导致两侧空白带的规则；保留 `safe-area` 与小边距。
- [x] 1.2 确认 `HomeView` 等非对局页不受破坏性影响（必要时仅用路由作用域 class 区分样式）。
- [x] 1.3 与现有 `layout--mobile-landscape` 全局样式交叉检查，避免双重 padding 挤压牌桌。

## 2. 音频控件：归入对局壳层

- [x] 2.1 将语音 / BGM 控件从「纯视口悬浮」改为 **对局页顶栏内**（或与顶栏右对齐、与牌桌同宽列），并添加稳定 **`data-testid`**。
- [x] 2.2 处理路由切换：`/` 与 `/game` 之间不出现 **两套重复** 音频按钮；非对局页保留原可发现性。
- [x] 2.3 核对 `z-index`：不低于教练 / 聊天遮挡逻辑需求，且不盖住竖屏旋转引导层规则。

## 3. 自动化测试

- [x] 3.1 **Playwright**：对局路由下断言 **游戏主容器宽度** 相对视口宽度 ≥ 约定阈值（如 0.92）；断言音频控件 **包围盒** 落在 `game-container`（或顶栏）内。
- [x] 3.2 **Vitest**：若引入路由壳层辅助函数或布局常量，补充单元测试；否则在 `tasks.md` / PR 说明中注明「布局以 E2E 为准」并保证至少一条 Vitest 覆盖新增纯函数。
- [x] 3.3 合并前本地执行：`client` `npm run build`、`npm run test`；`npm run test:e2e`（按 README 启动前后端或使用既有脚本）。

## 4. 文档与交付

- [x] 4.1 `README.zh-CN`（或客户端 README）片段：合并前须执行的构建与测试命令（与仓库脚本一致）。
- [x] 4.2 自检：宽屏 / 笔记本 / DevTools 移动横屏各看一眼无明显回归。
